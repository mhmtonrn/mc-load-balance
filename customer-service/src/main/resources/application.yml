spring:
  application:
    name: customer-service

server:
  port: 8092

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

logging:
  file: target/${spring.application.name}.log